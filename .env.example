# Base URL of the ICT Chamber Kanban backend API (see Project_Backend_descriptions.md).
# Must be reachable from the browser and must allow this app's origin via ALLOWED_CORS_ORIGINS
# on the backend.
VITE_API_BASE_URL=http://127.0.0.1:8000
